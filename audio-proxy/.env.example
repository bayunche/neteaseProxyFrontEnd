# 端口配置
PORT=3001

# 环境配置
NODE_ENV=production

# Redis配置（可选）
REDIS_PASSWORD=audioProxyRedis2024
REDIS_HOST=redis
REDIS_PORT=6379

# 域名配置（替换为你的实际域名）
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# SSL证书路径（如果使用Nginx）
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem